<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br>
    <%= f.text_field :id %>
  </div>
  <div class="field">
    <%= f.label :sc_id %><br>
    <%= f.text_field :sc_id %>
  </div>
  <div class="field">
    <%= f.label :topic_n %><br>
    <%= f.text_field :topic_n %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
